# Intentional bait for the secret-pattern check. NOT a real secret — repeating
# 'X' / 'TEST' tokens defeat GitHub's entropy heuristic but still match the
# `\bsk_live_[A-Za-z0-9]{16,}\b` regex in scripts/validate-registry.mjs.
STRIPE_KEY=sk_live_TESTTESTTESTTESTTESTXX
